VITE_API_BASE_URL=http://localhost:8000/api/v1
# Mesmo Client ID OAuth configurado em GOOGLE_OAUTH_CLIENT_ID no backend
# (Render) — sem isso, o botão "Entrar com Google" fica desabilitado.
VITE_GOOGLE_OAUTH_CLIENT_ID=

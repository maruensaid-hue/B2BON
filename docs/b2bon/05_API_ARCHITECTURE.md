# 05 — API Architecture, versionamento, namespaces (§11, §64–68)

> **Placeholder criado na Fase 0.** Conteúdo completo será escrito na
> fase responsável. Não contém decisões ainda.

- **Fase responsável**: 3
- **Estado atual / ponto de partida**: Hoje: `/api/v1/*` com 55 routers em PT (`/saude-contas`, `/cadencias`…); JWT + chave de parceiro; sem idempotency keys, sem API pública de produto (só provisionamento em `docs/api-parceiros.md`).
